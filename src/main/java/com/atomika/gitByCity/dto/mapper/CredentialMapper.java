package com.atomika.gitByCity.dto.mapper;

import com.atomika.gitByCity.dto.Credential;
import com.atomika.gitByCity.entity.CredentialEntity;
import org.mapstruct.Mapper;
import org.mapstruct.Mapping;
import org.mapstruct.factory.Mappers;

import java.util.List;

@Mapper(componentModel = "spring")
public interface CredentialMapper {

//    CredentialMapper INSTANCE = Mappers.getMapper(CredentialMapper.class);

//    CredentialEntity dtoToEntity (Credential credential);
//
//    Credential entityToDto (CredentialEntity credentialEntity);
//
//    List<Credential> toList (List<CredentialEntity> credentialEntityList);


}
